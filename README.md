# Basic Firebase Test Lab results parser 

## Usage
- `SOURCE_PATH` 
```
- python testlab.py --source "${SOURCE_PATH}" --destination public --build "${VERSION_NAME}" --version "${VERSION_CODE}" --pipeline "${CI_PIPELINE_ID}" --commiter "${GITLAB_USER_EMAIL}"
```
